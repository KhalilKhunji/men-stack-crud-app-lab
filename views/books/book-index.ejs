<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Index</title>
</head>
<body>
    <h1>Books!</h1>
    <a href="/books/new">Add New Book</a>
    <ul>
      <% books.forEach(book => { %>
        <li><a href="/books/<%= book._id %>"> <%= book.title %> - <%= book.author %></a></li>
      <% }); %>
    </ul>
</body>
</html>